<Window x:Class="InoriDock.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:pu="clr-namespace:InoriDock.Public"
        xmlns:do="clr-namespace:InoriDock.Public.DockbarComponents"
        Title="MainWindow"
        Width="1200"
        Height="200"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="Transparent"
        Style="{StaticResource WindowDefaultStyleKey}"
        mc:Ignorable="d">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding WindowLoadedCommond}" CommandParameter="nihao" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Border BorderBrush="Yellow" BorderThickness="3">

        <Border Padding="0"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                BorderBrush="Blue"
                BorderThickness="1">
            <StackPanel HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        do:Dock.IsDockEnabled="True"
                        Background="Transparent"
                        Orientation="Horizontal">
                <do:DockItem Width="80" Height="80" Content="{Binding Index, RelativeSource={RelativeSource self}}" />
                <do:DockItem Width="80" Height="80" Content="{Binding Index, RelativeSource={RelativeSource self}}" />
                <do:DockItem Width="80" Height="80" Content="{Binding Index, RelativeSource={RelativeSource self}}" />
                <do:DockItem Width="80" Height="80" Content="{Binding Index, RelativeSource={RelativeSource self}}" />
                <do:DockItem Width="80" Height="80" Content="{Binding Index, RelativeSource={RelativeSource self}}" />
                <do:DockItem Width="80" Height="80" Content="{Binding Index, RelativeSource={RelativeSource self}}" />
                <do:DockItem Width="80" Height="80" Content="{Binding Index, RelativeSource={RelativeSource self}}" />
                <do:DockItem Width="80" Height="80" Content="{Binding Index, RelativeSource={RelativeSource self}}" />
                <do:DockItem Width="80" Height="80" Content="{Binding Index, RelativeSource={RelativeSource self}}" />
                <do:DockItem Width="80" Height="80" Content="{Binding Index, RelativeSource={RelativeSource self}}" />
                <do:DockItem Width="80" Height="80" Content="{Binding Index, RelativeSource={RelativeSource self}}" />
                <do:DockItem Width="80" Height="80" Content="{Binding Index, RelativeSource={RelativeSource self}}" />
            </StackPanel>
        </Border>
    </Border>

</Window>